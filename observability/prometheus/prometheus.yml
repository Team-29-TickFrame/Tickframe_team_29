global:
  scrape_interval: PROMETHEUS_SCRAPE_INTERVAL_PLACEHOLDER
  evaluation_interval: PROMETHEUS_EVALUATION_INTERVAL_PLACEHOLDER

scrape_configs:
  - job_name: tickframe-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:BACKEND_PORT_PLACEHOLDER
